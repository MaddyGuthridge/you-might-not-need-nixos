# What the f*ck is a flake?

* You know how I complained about NixOS being so slow to rebuild?
* Like a `package-lock.json` in Node, it stores resolved dependencies, meaning 
  that they don't need to be recalculated every time.
* Sounds great right?
