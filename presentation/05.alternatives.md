# So what is NixOS up against?

* `mise` for development environment management. A single tool to install 
  and manage all your development tools.
* `docker` for consistent and easily-deployable builds of software, allowing for
  an isolated development when developing, building and deploying of software.
* But how do they compare?
* There's only one way to find out...
